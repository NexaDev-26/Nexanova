# NexaNova Environment Variables
# Copy this file to .env and update with your values

# Server Configuration
PORT=5000
NODE_ENV=development

# Security
# Generate a secure secret: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-secret-key-change-in-production

# CORS Configuration (comma-separated for multiple origins)
# For development, leave empty to allow all origins
# For production, specify your domain: https://yourdomain.com,https://www.yourdomain.com
CORS_ORIGINS=

# Frontend API URL (optional - auto-detected if not set)
# Only needed if frontend and backend are on different domains
REACT_APP_API_URL=http://localhost:5000/api

# Database (SQLite - auto-created in backend/data/)
# No configuration needed for SQLite

# WebSocket (enabled by default)
# No configuration needed